1. Check what's changed
git status

# 2. Stage the files you want to save (add them to the "next commit")
git add .              # the dot means "all changed files"

# 3. Commit — save a snapshot with a message
git commit -m "Add data ingestion script"

# 4. Push — upload your commits to GitHub
git push
pip install
